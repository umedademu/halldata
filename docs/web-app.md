# Webサイトの現状

## 画面構成

- `apps/web` に `Vercel` 向けのサイトを用意しています
- ページの流れは `店舗一覧 → 機種一覧 → 台データ` です
- `Aパーク春日店` の店舗ページ上部には、`狙い度分析` ページへの導線があります
- ブラウザーのタブ名は固定ではなく、`店舗一覧`、各店舗の `機種一覧`、`狙い度分析`、各機種の `台データ` に応じて切り替わります
- `店舗一覧` では、保存済みの店舗名だけを表で見られます
- `店舗一覧` では、店舗URLだけを登録待ちとして追加できます
- Web上で店舗名は取得せず、登録待ちURLはGUIアプリの更新または定期取得で店舗名を補完します
- `機種一覧` では、各機種ごとの最新記録日を基準に、保存済みの機種をリンク付きの表で見られます
- `台データ` では、同じ台番のデータを日付ごとに比較できます
- トップページ、店舗ページ、狙い度分析ページ、機種ページの上部見出しは、トップページの `保存済み店舗` と同程度の文字サイズで表示します

## データの読み出し

- データ元は `Supabase` の `stores`、`machine_daily_results`、`store_machine_summaries`、`store_machine_daily_details` です
- `apps/web` は `SUPABASE_URL` と `SUPABASE_SERVICE_ROLE_KEY` を使って読み出します
- 店舗URLの登録待ち追加では、`stores` にURLを保存します
- ローカル確認時は、ルートの `.env.local` も読めるようにしています
- 本番の `Vercel` では、同じ環境変数を `Vercel` 側に設定する想定です
- 機種一覧用の要約表名を変えたい場合は、`.env.local` または `Vercel` 側に `SUPABASE_MACHINE_SUMMARIES_TABLE` を入れると切り替えられます
- 台データページ用の詳細表名を変えたい場合は、`.env.local` または `Vercel` 側に `SUPABASE_MACHINE_DAILY_DETAILS_TABLE` を入れると切り替えられます

## 表示について

- `PC` と `スマホ` の両方で、左端の日付列を固定した横長の比較表で見られます
- `スマホ` では店舗一覧、機種一覧、台データ比較の表を横に動かしながら確認します
- 店舗一覧は `stores` だけを読み、台データ行は読みません
- 機種一覧は、まず `Supabase` の `store_machine_summaries` を読み、保存済みの機種一覧をそのまま表示します
- その要約表が無い環境では、ローカル実行時だけ `local_data` にある全機種取得済みの索引を見つけられる場合に、その索引と保存済みの一日分ファイルを使います
- どちらも使えない環境では、最後の手段として `Supabase` の保存済み台データから機種ごとの最新日を集計します
- 機種一覧の台数、平均差枚、平均G数、平均出率は、それぞれの機種にとって最新の日一日分をもとに表示します
- 機種一覧は、まず機種ごとの最新日が新しい順、その次に台数の多い順で表示します
- `ゴーゴージャグラー3` と `ゴーゴージャグラー３` のような機種名の表記ゆれは、設定ファイルをもとに同じ機種としてまとめて表示します
- ただし `ネオアイムジャグラーEX` と `SアイムジャグラーＥＸ` は別機種として分けて表示します
- この2機種は、最新取得で `SアイムジャグラーＥＸ` と確定した台番があれば、その台番の過去データも同じ機種へ移動したうえで表示します
- サイトセブン由来で `Supabase` に差枚が入っていない行でも、機種ごとの設定ファイルと `G数`、`BB`、`RB` から `差枚` を計算し、四捨五入した整数として補って表示します
- 差枚の計算条件はルートの `config/machine_difference_rules.json` にまとめてあり、今は `ネオアイムジャグラーEX`、`SアイムジャグラーＥＸ`、`マイジャグラー`、`ゴーゴージャグラー`、`ファンキージャグラー`、`ミスタージャグラー`、`ジャグラーガールズ`、`ウルトラミラクルジャグラー`、`ハッピージャグラー` を登録済みです
- 台データ比較では、横幅を抑えるために表内の日付を `YY/MM/DD` 形式で表示し、日付の隣に曜日を表示し、数値の桁区切りを省いています
- 台データ比較は、まず `store_machine_daily_details` に保存した日付ごとの完成データを読みます
- その詳細表が無い環境では、最後の手段として `machine_daily_results` の生データから組み立てます
- 台データ比較では、チェックボックスで表示する列を選べます
- 台データ比較の `出率`、`BB率`、`RB率` は初期状態では非表示です
- アイムジャグラーEX系、スマスロ ハナビ、新ハナビ、スマスロ サンダーV、バーサスリヴァイズ、ハナハナホウオウ、ファンキージャグラー2、マイジャグラーV、ハッピージャグラーV、ゴーゴージャグラー、ジャグラーガールズ、ウルトラミラクルジャグラー、ミスタージャグラーの台データ比較では、`G数`、`BB`、`RB` から別アプリと同じ計算方法で設定を推測し、`設定` 列に加重平均値を表示します
- 対応機種の台データ比較では、`データ推測`、`G数による推測`、`特定日6あり` の比重を入力できます
- `G数による推測` は標準で `6000G` から `9000G` までを指数 `1.5` で評価し、指数を大きくすると後半ほど評価が伸びやすくなります
- `特定日6あり` は、店舗ごとに保存している特定日行だけ、その機種の中で最も良い台を高く見る相対評価として使います
- `特定日6あり` の比較に使う元の評価は、`G数による推測` がオフならデータ推測だけ、オンならデータ推測とG数推測を合わせた値です
- 機種名に `ジャグラー` を含む対応機種では、初期値はデータ推測100%、G数による推測と特定日6ありをオフにします
- ジャグラー以外の対応機種が8台以下の場合、初期値はデータ推測20%、G数による推測40%、特定日6あり40%で、G数と特定日6ありをオンにします
- ジャグラー以外の対応機種が9台以上の場合、初期値はデータ推測80%、G数による推測20%、特定日6あり0%で、特定日6ありをオフにします
- 対応機種では、該当台番の表示中の列にポインターを合わせると各設定の推測割合を確認できます
- 対応機種では、推測値が4.0以上は黄色系、4.5以上は緑系、5.0以上は赤系の単色背景と太字で、該当日の該当台番にある表示中の列を強調します
- 日付末尾やゾロ目による行の強調と設定推測による強調が重なった場合は、設定推測による強調を優先します
- `Aパーク春日店` の `SアイムジャグラーＥＸ` `ネオアイムジャグラーEX` `マイジャグラーV` `ゴーゴージャグラー３` `ファンキージャグラー２ＫＴ` `ミスタージャグラー` `ジャグラーガールズSS` では、台データ比較に `狙い度` 列を追加しています
- `狙い度` は、その日付を含む直近7営業日だけを使う固定点方式の絶対評価で、0から100の整数として表示します
- 点数は `7日負け日数` `現在連敗数` `7日負け絶対量合計` `7日差枚合計` `補填率` `7日最大勝ち` `当日差枚` `当日推測設定` の8項目を足し合わせて作ります
- `狙い度` の計算では他の台との比較を行わず、店内順位や日内比率は使いません
- 店休日は「店内の全機種全台が空欄の日」を除外して扱い、7営業日が揃わない行や途中の日が欠ける行は `狙い度` を空欄にします
- 当日の `設定` を出せない行は、`狙い度` 計算では 0 として扱います
- 今回の `狙い度` 自体は数値表示とCSV出力だけに留めますが、`狙い度分析` では `翌営業日設定` が `4.00` 以上の行を黄色、`4.50` 以上を緑、`5.00` 以上を赤で強調します
- `Aパーク春日店` には別ページで `狙い度分析` を用意し、対象7機種をまとめた絶対評価の高得点台を表示します
- `狙い度分析` のURLは `stores/[storeId]/hunt-analysis` です
- `狙い度分析` の初期表示は保存済み最新日の集計で、過去の集計日に切り替えると、その翌営業日の実績もあわせて確認できます
- `狙い度分析` では `何位まで表示` をユーザーが指定でき、初期値は `20` です
- `狙い度分析` の `何位まで表示` はブラウザー側へ保存し、次回以降も前回使った値を復元します
- `狙い度分析` には、指定条件に合う台の翌営業日実績を機種別に集計する `翌営業日バックテスト` を追加しています
- バックテストの期間は `狙い度を出した日` を基準に扱い、直近日数指定と日付範囲指定のどちらでも絞れます。初期値は `直近90日` です
- バックテストの期間は `翌営業日` ではなく `狙い度を出した日` に対して効き、差枚、G数、平均設定は条件一致台の `翌営業日実績` だけを集計します
- バックテストの機種選択は複数チェック式で、初期状態では現状の対象7機種をすべて選択します
- バックテストの順位条件は開始順位と終了順位で指定でき、例えば `1位から3位` のように日ごとの順位範囲で翌営業日の実績を集計します
- 順位条件は `全機種順位` と `機種内順位` を切り替えられます
- `機種内順位` を選んだ場合は、その日に選択中の各機種それぞれの中で上位を拾うため、例えば3機種を選んで `1位から3位` にすると、各日で最大9台分が条件一致になります
- バックテストの狙い度条件は下限値で指定でき、例えば `90以上` のように日ごとの狙い度下限で翌営業日の実績を集計します
- 順位条件と狙い度条件は片方だけでも両方でも使え、両方を入れた時は `両方一致` と `どちらか一致` を選べます
- バックテストでは `グラフ表示` のオンとオフを選べ、初期状態はオンです
- グラフ表示をオンにすると、`翌営業日の日付` を横軸、`その時点までの累積差枚` を縦軸にした折れ線グラフを表示します
- バックテストの結果は `機種別` と `総計` で分けて表示し、`条件一致台数` `狙い度` `実績集計台数` `合計差枚` `合計G数` `機械割` `平均設定` を確認できます
- バックテスト結果の `狙い度` は、条件一致した台の狙い度の平均値です
- バックテストの `機械割` は、各行の `合計G数` と `合計差枚` から `1Gあたり3枚投入` として計算します
- バックテスト結果には `この条件を目印にする` ボタンを追加し、保存した目印条件を同じ店舗の `狙い度上位` 一覧へ反映できます
- 目印条件の強調判定には、機種選択、順位条件、順位の見方、狙い度条件、両条件を併用した時の一致方法を使います
- バックテストの期間は目印の表示内容には残しますが、`狙い度上位` 一覧で強調するかどうかの判定には使いません
- `狙い度上位` 一覧では、目印条件に一致する行を強めに、不一致の行を控えめに表示します
- 翌営業日の実績がまだ無い台は `条件一致台数` には含めますが、`実績集計台数` と差枚合計などの実績集計からは外します
- `狙い度分析` では `狙い度`、`機種名`、`台番`、`翌営業日差枚`、`翌営業日G数`、`翌営業日BB`、`翌営業日RB`、`翌営業日合成`、`翌営業日設定` を初期表示し、`翌営業日出率`、`翌営業日BB率`、`翌営業日RB率` はチェックボックスで追加表示できます
- `狙い度分析` の `機種名` を押すと、その店舗の該当機種の `台データ` ページへ移動できます
- `狙い度分析` の一覧は、`翌営業日設定` が `4.00` 以上の行全体を、機種ごとページの `設定` 列と同じ色で塗り分けます
- 対応機種の台データページでは、設定別の `BIG確率`、`REG確率`、`合成確率` を表示します
- 台データ比較は表示列を減らすと列幅と文字を大きくし、横幅は5台分の表示を上限にします
- 台データ比較の日付列は、列幅を広げても日付が見切れないよう固定幅を確保します
- 台データ比較では、台番と台番の境界が分かるように、各台番の右端の縦線を少し太く表示します
- 台データページでは、日付末尾の `0` から `9`、`ゾロ目`、曜日を複数選び、該当日だけに絞るか、全日を表示したまま該当行だけを強調できます
- 曜日は日曜から土曜まで7つあり、初期状態ではすべて未選択です
- 台データページで選んだ特定日の末尾、ゾロ目、曜日は、店舗ごとの `stores` の設定として自動保存し、次回以降の表示に使います
- 曜日を保存するには、`stores` に `event_weekdays` の数値配列列を用意します。列がない環境でも画面上の切り替えはできますが、曜日だけは再読み込み後に初期状態へ戻ります
- 台データページの表示方法は初期状態で `強調` になり、必要に応じて `絞込` に切り替えられます
- 台データページの絞り込みと強調は、ページ全体を読み込み直さずに画面内で切り替えます
- 台データ比較の表は、「CSV出力」ボタンで表示中の列をCSVファイルとしてダウンロードできます
- 台データ比較のCSVには `曜日` 列と `特定日` 列を出し、画面で選んだ末尾、ゾロ目、曜日に該当する日は `はい`、それ以外の日は `いいえ` として確認できます
- 日付は保存済みの `YYYY-MM-DD` を基準に扱い、環境による時差で前日にずれないようにしています
- 各ページには前の一覧へ戻るための導線を置いています

## 軽量化について

- `Supabase` から同じ条件で読み出した結果は、標準で60秒だけ使い回します
- 機種一覧は、重い全件集計を避けるため、`store_machine_summaries` を最優先で使います
- 台データページは、重い行データの組み立てを避けるため、`store_machine_daily_details` を最優先で使います
- ローカル実行で `local_data` の全機種取得済み索引を使える場合、機種一覧は `Supabase` の大量読み出しを避けます
- 取得結果の使い回し時間は、必要に応じて `HALLDATA_FETCH_CACHE_TTL_MS` でミリ秒単位に変更できます
- 外部書体の読み込みをやめ、端末に入っている日本語書体を使います
- 重いぼかし背景と初期表示時の動きを外し、表の描画を優先しています

## 実行方法

1. `apps/web` で `npm install` を実行する
2. `apps/web` で `npm run dev` を実行する
3. 必要に応じて `npm run build` で本番用の確認をする
